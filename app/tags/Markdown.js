'use strict';

const HTML = `
<h1>Markdown Parser</h1>
`;

function Markdown() {
    this.on('mount', () => {
    });

    this.on('unmount', () => {
    });
}

export default {
    html: HTML,
    constructor: Markdown
};
